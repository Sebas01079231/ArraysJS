// Sintaxis comun
const numeros = [20, 20, 30];

// Sintaxis de arreglo poco usual
const meses = new Array('Enero', 'Febrero', 'Marzo');

console.log(numeros);
console.log(meses);

// Arreglo con datos de todo tipo
const deTodo = ['hola', 10, true, 'si', null, {nombre: 'Sebas', trabajo: 'dev'}, [1,2,3] ];
console.log(deTodo); 